#import { Query, Connection } into Ethereum from "w3://ens/ethereum.web3api.eth"
#import { Query } into Logger from "w3://ens/js-logger.web3api.eth"
#import { Query } into Coingecko from "w3://ens/rinkeby/coingecko.defiwrapper.eth" 
#import { Query } into DateTime from "w3://ens/datetime.eth"
#import { Query } into Gelato from "w3://ens/gelato.eth"

type Query implements Gelato_Query{}

# Only modify here.
type UserConfig
